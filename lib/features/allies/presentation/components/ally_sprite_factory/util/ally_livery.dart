import 'package:flutter/material.dart';

const kAllyAccent = Color(0xFF00E5FF);

/// Shared hull/accent livery colors used across every ally sprite paint
/// function so they all read as "ours" at a glance (see `kHomeAccentColor`).
const kAllyHull = Color(0xFF2B3A42);
const kAllyHullDark = Color(0xFF172126);
